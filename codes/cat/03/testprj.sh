#!/bin/bash 

#Using the cat Command to Display Line Numbers
#With the cat command you can also display the contents of a file 
#along with line numbers at the beginning of each one. 
#To use this feature, use the -n option with cat command:
#cat -n filename.txt
cat -n filename.txt

