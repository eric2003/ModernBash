#!/bin/bash 

#Using the cat Command to Number Non-Empty Lines
#To display non-empty lines with line numbers printed before them use the -b option. 
#Remember the  -b option will override the -n option:
#cat -b filename.txt

cat -b filename.txt

